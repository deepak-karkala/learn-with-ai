fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.7.2
pydantic-settings>=2.5.2
email-validator>=2.0.0
python-dotenv==1.0.0
python-multipart>=0.0.9
httpx>=0.28.1
google-genai>=1.17.0
google-adk>=1.4.2
openai>=1.40.0

# Security and authentication
bcrypt>=4.1.0
pyjwt>=2.8.0
cryptography>=41.0.0
python-jose[cryptography]>=3.3.0

# Voice processing
wordsegment>=1.0.0

# Optional for production (commented out for MVP demo):
# redis>=5.0.0                     # Only if using Redis for caching
# sqlalchemy>=2.0.0               # Only if using PostgreSQL
# psycopg2-binary>=2.9.0          # Only if using PostgreSQL

# Networking
aiohttp>=3.9.0


# REMOVED - Too large for serverless deployment (250MB limit):
# opik>=0.2.0                 # 11MB - Monitoring; use basic logging
# nltk>=3.8                   # Often brings numpy (100MB+)

# REMOVED - Dependency bloat from google-adk includes these:
# numpy                       # 109MB - Pulled by opik/litellm
# litellm                     # 41MB - Pulled by opik
# googleapiclient             # 90MB - Pulled by google packages

# NOTE: These large packages are transitive dependencies from google-adk.
# They cannot be easily removed without losing ADK functionality.
# Consider using Google Cloud Functions or a dedicated ML service for
# advanced features (voice, embeddings, LLM monitoring) in Phase 2+
